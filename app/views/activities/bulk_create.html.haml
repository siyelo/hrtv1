- title("Activities Bulk Create")

- content_for :sub_nav do
  = render 'shared/sub_nav_projects'

%h1.main_heading Activities Bulk Create
- @activities.each_with_index do |activity, index|
  .activity_box
    = render 'bulk_edit', :activity => activity, :response => @response

%br

= link_to "Finish and Continue", response_projects_path(@response), :class => 'create_button', :confirm => "Please check if all activities are saved. Are you sure you want to leave this page?"
