- title("New Activity")

%h1.main_heading Create New Activity

= error_messages_for :activity,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @activity, :url => response_activities_path(@data_response), :html => {:class => "basic_form"} do |f|
  = render 'form', :f => f
