- title("New Activity")

- content_for :sub_nav do
  = render :partial => 'shared/sub_nav_projects'

%h1.main_heading
  New Activity
  %span= render 'help'

= render 'activities/tab_nav', :activity => @activity


= error_messages_for :activity, :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @activity, :url => response_activities_path(@response), :html => {:class => "basic_form"} do |f|
  = render 'form', :f => f
