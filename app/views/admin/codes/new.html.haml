- title("New Code")

%ul.breadcrumb
  %li= link_to "Codes", admin_codes_path

%h1.main_heading Create New Code

= error_messages_for :code,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for [:admin, @code], :url => admin_codes_path, :html => {:class => "basic_form"} do |f|
  = render 'form', :f => f
