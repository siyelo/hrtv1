%table.standard
  %thead
    %tr
      %th Organization
      %th Total Spent
      %th Total Budget

  %tbody
    - organizations.each do |organization|
      %tr{:class => cycle("even", "odd")}
        %td= organization.name
        %td= n2c(organization.spent_sum, 'RWF')
        %td= n2c(organization.budget_sum, 'RWF')
