%h1.main_heading Edit data request

= error_messages_for :request, :header_message  => nil, :message => "Oops, we couldn't save your changes."

- form_for @request, :builder => CustomFormBuilder, :url => admin_request_path(@request), :html => {:class => 'basic_form js_form'} do |f|
  = render 'form', :f => f
