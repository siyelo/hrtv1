- title("New User")

%ul.breadcrumb
  %li= link_to "Users", admin_users_path

%h1.main_heading Create New User

= error_messages_for :user,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for [:admin, @user], :url => admin_users_path, :html => {:class => "basic_form"} do |f|
  = render 'form', :f => f
