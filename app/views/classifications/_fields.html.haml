- # amount     = assignment.nil? ? '' : assignment.amount
- # amount2     = assignment2.nil? ? '' : assignment2.amount
- cached_amount = assignment.nil? ? '' : assignment.cached_amount
- cached_label = cached_amount.blank? ? "" : "#{n2c(cached_amount)}"
- percentage = derive_percentage_from_amount(type, assignment)
- options = percentage_field_options(code, assignment, valid)

= text_field_tag "classifications[#{type}][#{code.id}]", h(percentage), {:size => 5, :class => "percentage_box"}.merge(options)
.inline_ratio %
.subtotal= cached_label