- title "Budgets"

- record_type = Activity

- content_for :sub_nav do
  = render :partial => 'layouts/sub_nav'

= render :super

%p= link_to "Next Step", other_costs_path, :class => "next right"

= render 'shared/data_entry_help', :record_type => record_type
