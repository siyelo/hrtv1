<% @grid = render :super %> 
<% if params[:action] == "table" #if did in index, would break AS
%>
  <%= @grid %>
<% else %>
  <%= 
    render :partial => "shared/data_entry_for_model_admin",
      :locals => { :record_type => Comment, :grid => @grid }
    %>
<% end %>
