- title("Edit Commodity")

%h1.main_heading Edit Commodity

= error_messages_for :commodity,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @commodity, :url => response_commodity_path(@data_response, @commodity), :html => {:class => "basic_form"} do |f|
  = render 'form', :f => f
