<%= "Know this is not yet fixed, providers and sources look the same" %>
<% @grid = render :super %> 
<%= 
  render :partial => 'shared/data_entry_for_model',
  :locals => {
    :record_type => Project,
    :grid => @grid
  }
%>
