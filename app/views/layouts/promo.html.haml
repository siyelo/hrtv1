-# main site landing page - large header

- content_for :body do
  %body.promo.landing
    = render "layouts/header"
    = yield(:tabs)
    #content
      .wrapper
        = yield

- content_for :footer do
  = render :partial => 'layouts/footer_landing'

= render 'layouts/app'