-# main site landing page - large header

- content_for :body do
  %body.promo.landing
    = render "layouts/header"
    = yield(:tabs)
    #content
      .wrapper
        = yield

    = render 'layouts/footer'

= render 'layouts/app'