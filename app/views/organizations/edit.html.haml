- title("Settings")

%h1.main_heading== #{@organization.name}: Settings

= render 'responses/getting_started'

= error_messages_for :organization,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @organization, :url => organization_path(@organization), :html => {:class => 'basic_form'} do |f|
  = render 'form', :f => f
