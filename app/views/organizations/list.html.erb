<%= link_to "Fix duplicate organizations", duplicate_admin_organizations_path if current_user.admin? %>
<% @grid = render :super %> 
<%= 
  render :partial => "shared/data_entry_for_model_admin",
    :locals => { :record_type => Organization, :grid => @grid }
%>
