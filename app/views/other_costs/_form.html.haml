.form_aside
  %h3 What's an Other Cost?
  %p Other Cost is ...
  %br
  %h3 How do I add more detail?
  %p In the Health Resource Tracker, you can break-down your Projects into of several smaller, more detailed, Other costs.
  %p For example if your project is (sub)implemented by multiple entities, you can create one or more Other Costs to represent this.

- f.inputs :class => 'wrap-80 inputs' do
  %li.last
    %h3 Basics
    %ul.dashboard_section
      = f.input :description, :input_html => { :rows => 3 }, :hint => "A general 1-2 sentence description of the purpose of the project. "
      = f.input :project_id, :label => "Project", :as => :select, :collection => current_user.organization.projects

  %li.last
    %h3 Budget (planned expenditure)
    %ul.dashboard_section
      = f.input :budget, :label => "Budget", :hint => "The total amount that you have spent in the past fiscal year."

  %li.last
    %h3 Past Expenditure
    %ul.dashboard_section

      = f.input :budget, :label => "Spend", :hint => "The total amount that you are planning to spend in the upcoming fiscal year."

      %li.amounts.last
        %label Quarterly spend*
        %ul
          = f.input :spend_q4_prev, :label => "Q4 08-09"
          = f.input :spend_q1     , :label => "Q1 09-10"
          = f.input :spend_q2     , :label => "Q2 09-10"
          = f.input :spend_q3     , :label => "Q3 09-10"
          = f.input :spend_q4     , :label => "Q4 09-10"

- f.buttons :class => 'wrap-80 buttons' do
  = f.commit_button @other_cost.new_record? ? "Create New Other Cost" : "Update Other Cost", :class => "last"
