#new_project_form.hidden
  %h1.main_heading
    New Project
    %span= render 'help'
  = error_messages_for :project,  :header_message  => nil, :message => "Oops, we couldn't save your changes."
  - semantic_form_for @project, :url => response_projects_path(@response), :html => {:class => "basic_form"} do |f|
    = render 'form', :f => f

#new_activity_form.hidden
  %h1.main_heading
    New Activity
    %span= render 'help'
  = error_messages_for :activity, :header_message  => nil, :message => "Oops, we couldn't save your changes."
  - semantic_form_for @activity, :url => response_activities_path(@response), :html => {:class => "basic_form"} do |f|
    = render 'shared/outlays/form', :f => f, :outlay => @activity
  	
#new_other_cost_form.hidden
  %h1.main_heading
    New Other Cost
    %span= render 'help'
  = error_messages_for :other_cost,  :header_message  => nil, :message => "Oops, we couldn't save your changes."
  - semantic_form_for @other_cost, :url => response_other_costs_path(@response), :html => {:class => "basic_form"} do |f|
    = render 'shared/outlays/form', :f => f, :outlay => @other_cost