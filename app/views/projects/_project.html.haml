%tbody
  %tr.head
    %td
      %h1
        = link_to "#{h project.name}", edit_response_project_path(@response, project)

        - project_errors = get_project_errors(project)
        - if project_errors.present?
          = image_tag "warning_12.png", :title => project_errors, :class => 'tooltip'
      - link_to new_response_activity_path(@response, :project_id => project.id), :class => "add_row" do
        = image_tag"new.png"
        Add activity

      - link_to "#", :class => "add_row upload_btn" do
        = image_tag "new.png"
        Import / Export

      = render 'activities_import', :project => project, :can_export => project.activities.present?

    %td.wrap-10
      - link_to edit_response_project_path(@response, project), :class => 'add_row right' do
        = image_tag "pencil_10.png"
        edit
  = render 'activities', :response => @response, :project => project, :activities => project.normal_activities

  = render 'other_costs', :response => @response, :project => project, :other_costs => project.other_costs
