- title("Edit Project")

- content_for :sub_nav do
  = render :partial => 'layouts/sub_nav'

%h1 Edit Project

= error_messages_for :project,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @project, :url => projects_path do |f|
  = render 'form', :f => f

= render 'comments'
