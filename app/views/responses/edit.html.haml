- title("Edit Data Response")

%h1 Response Settings

= error_messages_for :data_response,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @data_response, :url => responses_path do |f|
  = render 'form', :f => f

= render 'comments'
