- title("New Response")

%h1.main_heading New Response

= error_messages_for :data_response,  :header_message  => nil, :message => "Oops, we couldn't save your changes."

- semantic_form_for @data_response, :url => responses_path, :html => {:method => :post} do |f|
  = render 'form', :f => f