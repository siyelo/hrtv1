%li{:rel => "#{code.id}"}
  = code.short_display
  - unless code.leaf?
    %ul
      - @coding_tree.cached_children(code).sort_by(&:short_display).each do |child|
        = render 'shared/code_row', :code => child
