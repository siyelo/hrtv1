<% if params[:action] == "table" #if did in index, would break AS
%>
  <%= grid %>
<% else %>
  <%= render :partial => 'shared/top_data_entry',
   :locals => { :record_type => record_type } %>
  <%= grid %>
  <%= render :partial => 'shared/bottom_data_entry',
    :locals => { :record_type => record_type } %>
<% end %>
