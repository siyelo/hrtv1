<% @grid = render :active_scaffold => "funding_flows", :constraints => @constraints, :label => @label %>
<%= render :partial => 'shared/data_entry_for_model',
  :locals => {
  :record_type => record_type,
  :human_plural_name_for_entity => human_plural_name_for_entity,
  :grid => @grid
  }
%>
