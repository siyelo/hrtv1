- unless activity.sub_activities.empty?
  %table.sub_activities
    %thead
      %tr
        %th Implementer
        %th Current Budget
        %th Expenditure
    %tbody
      /TODO sort by budget & calculate %'s if missing
      - activity.sub_activities.find(:all, :include => :provider).each do |sub_activity|
        %tr.sub_activity.entry_header
          %td=  sub_activity.implementer.try(:name)
          %td= n2cnds(universal_currency_converter(sub_activity.budget, sub_activity.currency, sub_activity.data_response.currency), sub_activity.data_response.currency)
          %td= n2cnds(universal_currency_converter(sub_activity.spend, sub_activity.currency, sub_activity.data_response.currency), sub_activity.data_response.currency)
        %tr.sub_activity.entry_main
          %td.s_border{:colspan => 100}= sub_activity.text_for_provider
