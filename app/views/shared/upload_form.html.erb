<p>Create one or more <%= @human_record_name %> from a CSV file. You can save an Excel spreadsheet as a CSV file through File -> Save As, then select comma separated values (CSV) in the file format box under the file name.</p>

<% form_tag({:action=> :create_from_file}, :multipart => true) do %>
  <p>
  <%= label_tag :file, "File" %>
  <%= file_field_tag :file %>
  <%= submit_tag "Upload from File" %>
  </p>
<% end %>
