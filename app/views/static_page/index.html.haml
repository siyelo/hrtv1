- content_for :head do
  = stylesheet_link_tag 's3slider.css', :media => 'screen'
  :javascript
    jQuery(document).ready(function(){
      jQuery('#s3slider').s3Slider({
        timeOut: 4000
      });
    });


- content_for :header do
  = render :partial => '/layouts/header'

-content_for :main do
  #content
    .wrapper
      .slider
        #s3slider
          %ul#s3sliderContent
            %li.s3sliderImage
              %img{ :src => "images/slider/1.png" }
              %span.top
                Resources going where they're most needed
            %li.s3sliderImage
              %img{ :src => "images/slider/2.jpg" }
              %span.top
                To improve and save lives
            %div.clear.s3sliderImage
      .text
        %h3 Better Information.
        %h3 Better Decisions.
        %p This database tracks resource flows in the health sector from their many sources to the ultimate health function they serve. Information about health spending, both planned and realized, are essential for evidence-based budgeting, planning and policy-making.
        %p It was created under the stewardship of the Resource Tracking Steering Committee, chaired by MINISANTE, with extensive input from CNLS. It was implemented by Health Systems 20/20, a global project funded by USAID, aided by many development partners, especially USAID, UNAIDS and CHAI.
